---
import MainLayout from './MainLayout.astro';
import Footer from '../shared/Footer.astro';

const { title, description, path } = Astro.props;
---

<MainLayout title={title} description={description} path={path}>
    <slot />
</MainLayout>
